# Local environment snapshots contain secrets and must never be uploaded by the CLI.
**/.env
**/.env.*

# Vercel installs and builds from the workspace manifests; local artifacts are both
# enormous and platform-specific (especially Sharp/libvips binaries).
**/node_modules
**/.next
**/.turbo
**/coverage
**/*.tsbuildinfo
.git
