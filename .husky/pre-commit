if [ "$SKIP_PRE_COMMIT" = "true" ]; then
  echo "🔍 Skipping pre-commit checks..."
  exit 0
fi

echo "🔍 Running pre-commit checks..."

# Run lint-staged for staged files across the monorepo
# This includes linting, formatting, and type checking for staged files
echo "� Linting, formatting, and type-checking staged files..."
pnpm lint:staged

echo "✅ Pre-commit checks passed!"
